name: CI Pipeline

on:
  push:
    branches: -spring_boot
  pull_request:

jobs:
  print_job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Hello from GitHub Actions!"
